services:
  web:
    # カレントディレクトリの Dockerfile を使ってビルド
    build: .

    # ホスト 3000 番ポート → コンテナの 8501 番ポート（Streamlit）
    ports:
      - "3000:8501"

    # 必要に応じてホットリロードしたい場合はコメントアウトを外す
    # volumes:
    #   - .:/app
